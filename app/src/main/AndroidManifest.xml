<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.checkpoint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checkpoint">
        <activity android:name=".Admin_AllRegistrations"></activity>
        <activity android:name=".DisplayRegistrations" />
        <activity android:name=".PoojaRegister" />
        <activity android:name=".ShowActivityUser" />
        <activity android:name=".MyAdapterUser" />
        <activity android:name=".User" />
        <activity android:name=".MyAdapter" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".AddPooja" />
        <activity android:name=".Register" />
        <activity android:name=".Admin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_80Jeicr0dG4uXD" />
    </application>

</manifest>